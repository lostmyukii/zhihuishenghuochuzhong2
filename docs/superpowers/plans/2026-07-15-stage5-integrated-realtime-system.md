# 阶段5一体化实时系统实施计划

1. 先扩展契约测试：固件完整执行器、5秒启动保护、安全覆盖、真串口网关、Web Serial、设备注册、语音白名单、执行器面板和日志。
2. 确认新契约能在当前 `0.3.2` 基线上失败，保留失败证据。
3. 固件升级至 `0.4.0`，实现风扇/舵机/继电器/蜂鸣器/RGB 真实应用值和安全启动窗。
4. 扩展手动覆盖，所有命令带 ID，安全状态始终优先，`auto` 可清除单项手动覆盖。
5. 将 Python 网关从 mock-only 扩展为 mock/真串口双路，保留固定本地端口 `18766/18767`。
6. 扩展 Dashboard：Web Serial、房屋热区、九个传感数据项、五个执行器、情境、告警、注册、语音文本、调试台、状态和日志。
7. 运行 Python/Node 全量回归、JavaScript 语法检查、PIO 编译、`git diff --check`。
8. 在外接电池关闭且用户确认后，只写 `0x10000` 应用区，独立校验并检查实时 `hello/telemetry/ack`。
9. 根据真板数据更新 `设计方案.md`、`开发文档.md`和 `AGENTS.md`，只提交本阶段文件并推送 `origin/main`。

